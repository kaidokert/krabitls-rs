repos:
-   repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
    -   id: trailing-whitespace
    -   id: end-of-file-fixer
    -   id: check-added-large-files
    -   id: check-merge-conflict
    -   id: check-yaml
    -   id: check-toml
    -   id: mixed-line-ending
        args: ["--fix=lf"]

-   repo: https://github.com/doublify/pre-commit-rust
    rev: v1.0
    hooks:
    -   id: fmt
        name: Fmt for krabitls
        args: ['--manifest-path=krabitls/Cargo.toml', '--']
    -   id: fmt
        name: Fmt for cortex_m_demo
        args: ['--manifest-path=cortex_m_demo/Cargo.toml', '--']

# Local hooks: actually build and test the library + the M3 example.
-   repo: local
    hooks:
    -   id: krabitls-clippy
        name: cargo clippy krabitls
        entry: cargo clippy --manifest-path=krabitls/Cargo.toml --tests --all-targets -- -D warnings
        language: system
        pass_filenames: false
        files: ^krabitls/.*\.(rs|toml)$
    -   id: krabitls-test
        name: cargo test krabitls (host, all-features)
        entry: cargo test --manifest-path=krabitls/Cargo.toml --all-features
        language: system
        pass_filenames: false
        files: ^krabitls/.*\.(rs|toml)$
    -   id: cortex-m-build
        name: "cargo build cortex_m_demo (thumbv7m: default + baseline + jedisct)"
        entry: sh -c "cargo build --manifest-path=cortex_m_demo/Cargo.toml --release --example krabitls --target thumbv7m-none-eabi && cargo build --manifest-path=cortex_m_demo/Cargo.toml --release --example krabitls --target thumbv7m-none-eabi --features baseline && cargo build --manifest-path=cortex_m_demo/Cargo.toml --release --example krabitls_jedisct --target thumbv7m-none-eabi --features jedisct"
        language: system
        pass_filenames: false
        files: ^(cortex_m_demo|krabitls|testdata)/.*\.(rs|toml|x|bin|hex)$
