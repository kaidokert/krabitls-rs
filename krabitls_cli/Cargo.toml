[package]
name = "krabitls_cli"
version = "0.2.0"
edition = "2024"
rust-version = "1.87"
autobins = false

[features]
# Cert validity-window checking is always compiled in krabitls; the CLI
# supplies a `SystemTimeSource` clock so it's active by default.
default = []
rsa = ["krabitls/rsa"]
# ECDSA P-256/P-384 server-cert verification.
ecdsa = ["krabitls/ecdsa"]
# Advertise TLS_CHACHA20_POLY1305_SHA256 alongside AES-128-GCM-SHA256.
chacha20 = ["krabitls/chacha20"]

[dependencies]
krabitls = { version = "0.4.0", path = "../krabitls", features = ["ip-host"] }
embedded-nal = "0.9"
embedded-io = "0.7"
std-embedded-nal = "0.4"
nb = "1"
rand_core = { version = "0.10", default-features = false }
log = "0.4"
env_logger = { version = "0.11", default-features = false, features = ["auto-color", "humantime"] }
getrandom = { version = "0.4", features = ["sys_rng"] }
zeroize = { version = "1.8", default-features = false }

[dev-dependencies]
krabitls_fixtures = { path = "../krabitls_fixtures" }

# The only CLI binary. Drives the `krabitls::client` facade over an
# `embedded-nal` stack (`std-embedded-nal` on host). No `required-features`:
# the Ed25519 / self-signed path builds without `rsa`.
[[bin]]
name = "krabitls_connect"
